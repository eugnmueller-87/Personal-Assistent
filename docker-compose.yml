services:
  bot:
    build: ./bot
    command: python main.py
    env_file: .env
    restart: unless-stopped

  pwa:
    build: ./bot
    command: uvicorn pwa_server:app --host 0.0.0.0 --port 8080
    env_file: .env
    ports:
      - "8080:8080"
    restart: unless-stopped
